# QA matrix

One row per check per machine. Fill by hand; a PR that claims a check passed must have its row here.

## Gamma spike (C0)

| Date | Machine | macOS | Auto-brightness | `set` return | read-back green max | Screen turned red? |
|---|---|---|---|---|---|---|
| 2026-09-09 | MacBookPro18,1 M1 Pro | 27.0 (26A5416b) | ON | 0 (success) | 0.0 | **pending — needs human eyes, structurally.** Tried two ways to verify automatically: (1) raw `screencapture` CLI failed outright (`could not create image from display`, no Screen Recording TCC grant); (2) a computer-use screenshot taken 2s into the red window *succeeded* but showed normal full-color menu-bar icons, not red. That is not a bug — macOS screen-capture composites from the pre-LUT framebuffer, so `CGSetDisplayTransferByTable` changes are invisible to *any* screenshot API by design (this is the same mechanism CLAUDE.md rule 4 relies on for "screenshots aren't tinted"). So no software check, with any permission, can ever confirm this — only a human looking at the physical panel can. Owner: please run `swift scripts/gamma_spike.swift` yourself and confirm the screen visibly flashed red for ~4s. |
| | MacBookPro18,1 M1 Pro | 27.0 (26A5416b) | OFF | | | pending — also needs "Automatically adjust brightness" turned off in System Settings → Displays first (a system-setting change left to the owner, not done automatically). |

## App checks

| Date | Version | Machine | macOS | Display(s) | Check | Result | Notes |
|---|---|---|---|---|---|---|---|
| 2026-09-09 | C1 (pre-0.1) | MacBookPro18,1 M1 Pro | 27.0 (26A5416b) | built-in | `xcodebuild test` | pass | 29/29 (started at 22; `/code-review high` found a real inverted-label bug + 9 others, a follow-up read found the debounced-save-lost-on-quit bug — 7 tests added across both) |
| 2026-09-09 | C1 | same | same | n/a | Fresh clone → `scripts/bootstrap.sh` → `xcodebuild test` | pass | Cloned the branch to a temp dir with no `Dimit.xcodeproj`, ran bootstrap, got 29/29. Confirms the project really is reproducible from `project.yml` alone, and that XcodeGen does not clobber the hand-written `Info.plist` (`LSUIElement` intact, `git status` clean after generating). |
| 2026-09-09 | C1 | same | same | n/a | Right-click menu validation + anchor geometry | pass | Verified by probe rather than by clicking (this session can't screenshot menus): `NSStatusBarButton.isFlipped == true`, so the `y = bounds.height` anchor drops the menu *below* the button as intended; `menu.update()` shows Quit enabled via the responder chain despite its nil target, and Settings correctly auto-disabled. |
| 2026-09-09 | C1 | same | same | built-in | App launches, no crash | pass | verified via `ps`, plus os_log line at launch |
| 2026-09-09 | C1 | same | same | built-in | Menu bar icon shows, toggles outline/filled | pass | confirmed via screenshot; SF Symbol placeholder, real icon is C3 |
| 2026-09-09 | C1 | same | same | n/a | Left-click opens popover; right-click shows menu | pass (logic), **not confirmed visually on-screen** | AppKit itself confirms the click event is received with the correct type, `togglePopover()` runs, and the popover reports `isShown=true` with a sane content size — but this session's virtual display could not screenshot the live popover: Dimit's frontmost status kept reverting to the terminal within about a second (likely this sandboxed display's window-focus handling for accessory apps), and the automation tool separately refuses to interact with a non-allowlisted app while it is frontmost. **Owner: please click the status item once for real and confirm the popover appears** — this is the one C1 behavior I could not verify end-to-end myself. |
| 2026-09-09 | C1 | same | same | n/a | Popover layout + string fit, en/uz/ru at 320pt | pass | Not verifiable via the live popover (see row above), so verified instead by hosting `PopoverView` in a real (off-screen-positioned) `NSWindow` with the actual compiled `.lproj` catalogs and rasterizing it — same AppKit drawing path a real popover uses, just not the literal status-item window. All three languages fit with no truncation or wrapping; Russian's longest string (`main.pwm_safe`, "Режим без мерцания (PWM)") fits on one line. Screenshots sent to the owner directly. |
| 2026-09-09 | C1 | same | same | n/a | State persists across relaunch | pass (automated) | `AppStateTests.test_persistence_roundTrips`; found and fixed a real bug in the process — `Persistence` was passing the app's own bundle ID as a `UserDefaults` suite name, which Foundation logs as invalid and silently does not work as a shared suite (there is no App Group entitlement, so it never could); fixed to use `.standard`. |

Checks to cover (from CLAUDE.md §8): 0K red on every display · ⌘⇧4 screenshot not red · QuickTime recording not red · Zoom/Meet share not red · Fallback-mode screenshot IS red · unplug while ON · sleep/wake · clamshell · fullscreen HDR video · quit restores · kill -9 then relaunch restores · idle CPU < 0.5% · PWM pinned state · brightness key re-pin · 10% brightness via overlay · hotkeys with another app frontmost · login item after reboot · no permission prompts · VoiceOver in en/uz/ru.

## C2 — gamma engine (2026-09-09, MacBookPro18,1 M1 Pro, macOS 27.0 26A5416b, built-in display only)

Screenshots can't show a gamma tint (see the C0 row above), so every check below verifies by reading the live gamma table back directly with `CGGetDisplayTransferByTable` — a precise, vision-free check of the actual OS-level state, not an inference. Verification ran against **the real compiled `Dimit.app` binary**, not a reimplementation: a standalone probe copies the actual `Dimit/Display/*.swift` and `Dimit/App/AppState.swift` source files (same technique as C1's popover render check) and drives them directly, and separately the real `Dimit.app` was launched with its persisted state pre-seeded to `isOn: true` via `UserDefaults(suiteName: "app.dimit.mac")` from outside the process.

| Check | Result | Notes |
|---|---|---|
| 0K sets green/blue to exactly 0, preserves red | pass | Real pipeline (AppState→DisplayCoordinator→Applier→GammaController), read back directly: baseline midpoint (0.500, 0.500, 0.500) → after ON at 0K (0.500, 0.0, 0.0). |
| OFF restores to baseline | pass | Same run, immediately after: back to (0.500, 0.500, 0.500), exact match. |
| Real `Dimit.app`, cold launch with `isOn: true` persisted, applies gamma with zero interaction | pass | Seeded `state.v1` externally, launched the actual binary, read gamma back: (0.500, 0.0, 0.0) — proves the reactive pipeline's initial `reapply()` in `DisplayCoordinator.init` works, not just the manual-toggle path. |
| ⌘⇧4-equivalent screenshot while genuinely red | pass | Confirmed gamma was red via direct readback *at the same moment*, then screenshotted: fully normal colors. Stronger than the C0 check — this time the tint was independently proven to be actually active when the screenshot was taken. |
| `SIGTERM` while ON → clean quit + restore | pass | Real app, `kill -TERM`, process exits, gamma back to baseline. |
| `kill -9` while ON → then check | pass, with a genuine surprise | On **this macOS 27 beta build**, WindowServer restores the gamma table to baseline **immediately** when the owning process dies via SIGKILL — reproduced twice, both via the probe and the real app binary. This was not assumed going in; CLAUDE.md's own cited bugs (FB19136488, FB22273730) are exactly why gamma behavior is known to be inconsistent across macOS versions and hardware, so **the "restore on launch, before anything else" safety net stays in the code regardless** — it's a no-op here (nothing to restore) and a real fix on whatever macOS version doesn't auto-clean-up this way. Do not assume this finding generalizes; re-check on macOS 13–26 during beta. |
| Fresh relaunch after `kill -9`, `isOn: true` still persisted | pass | Real app: baseline captured correctly as neutral (red channel matched the true baseline exactly, 0.50048876 — not some corrupted/double-tinted value), red state correctly reapplied. Confirms the restore-before-baseline-capture ordering is correct even though this specific machine didn't strictly need it this time. |
| Idle CPU | pass (quick check, not the full 5-min average) | `ps` samples at launch and 6s later: 0.0% both times. Owner should still confirm with a real 5-minute Activity Monitor average per CLAUDE.md §8. |
| `CGDisplayRegisterReconfigurationCallback` registers without error | pass | Confirmed via unified log: no error logged; probe additionally confirmed the API accepts the real callback + userInfo pointer and round-trips a UUID correctly. |
| Reconfiguration callback actually *fires* on a real plug/unplug | **pending** | No external monitor connected during this session. Owner: plug in the home monitor with the filter ON and confirm it picks up the warmth/dim within ~300ms, and that unplugging doesn't crash the app. |
| Sleep/wake re-applies within ~1s | **pending** | Can't safely trigger real system sleep in an unattended session. Owner: put the Mac to sleep and wake it with the filter ON, confirm the tint is still there (or reapplies) within about a second. |
| Multi-display | **pending** | Same blocker — only the built-in display was connected this session. |
| Slider feels instant | not independently measurable | Architecturally addressed: `Applier.diff` (`DimitTests/ApplierTests.swift`) confirms redundant `render()` calls with unchanged gamma produce zero work, so a slider drag within the same dim-floor-clamped range costs nothing extra. Actual perceived latency needs a human's hands on the real slider.

**Bug caught and fixed while writing `DisplayManager`, not found by review after the fact:** `deinit` originally called `CGDisplayRemoveReconfigurationCallback` with `userInfo: nil`, but registration used this instance's opaque pointer — a mismatch could leave a dangling registration pointing at freed memory the next time any display reconfigured.

**`/code-review high`, 8 parallel angles, 10 findings, all fixed** (see the PR for the full list): the two most significant were `verifyReadBack` only ever comparing the red channel (blind to exactly the green/blue corruption a Tahoe-class bug would cause) and a global-restore-ordering bug where `CGDisplayRestoreColorSyncSettings()` ran after per-display applies — invisible today since `Renderer`'s single global `isOn` flag can't produce a mixed toApply/toRestore diff, but `Applier` is already built and tested for that mix, and it would have silently wiped a still-on display's gamma the moment per-display control lands. Also fixed: a state-desync race in the "Restore Colours" button, failed gamma applies never being retried, a `MainActor.assumeIsolated` that could trap on an unproven thread guarantee (replaced with `Task { @MainActor in }`), a UUID-lookup fallback that could drift a display's baseline cache key on a transient failure, and a translation-process violation (shipped guessed uz/ru text instead of English + a review marker, per CLAUDE.md §5.1's explicit fallback). Re-ran the full integration probe against the real display after every fix: still passes. Fixed to pass the same pointer both times.

## C3 — PWM-Safe, brightness backends, extreme dim, Fallback mode (2026-09-09, same machine)

Private-framework verification came first, before writing any of the rest of this cycle: `dlopen`/`dlsym` against real symbols on this machine, watching actual return values.

| Check | Result | Notes |
|---|---|---|
| `DisplayServicesGetBrightness`/`SetBrightness` resolve and work | pass | Set to 1.0, read back exactly 1.0; set back to the original value, read back exactly that value. This is the backend PWM-Safe actually uses on this machine. |
| `CoreDisplay_Display_GetUserBrightness`/`SetUserBrightness` resolve | pass, but **functionally inert on this panel** | Both symbols link and can be called without crashing, but `get()` returns a constant `1.0` regardless of the display's actual brightness — verified by setting several different real values via DisplayServices and reading CoreDisplay's value each time; it never moved. Flagged in `BrightnessController.swift`'s doc comment: if a Studio Display/Pro Display XDR behaves the same way, this fallback's pin verification would falsely report success even if `set()` did nothing. **Needs a beta tester with one of those displays to check.** |
| `IORegistry` (`AppleARMBacklight`) brightness cross-check | pass to read, **does not track real changes** | None of `brightness`/`rawBrightness`/`BrightnessMilliNits`/`BrightnessMicroAmps` moved at all when the real brightness was set to several different values. Implemented per spec anyway (harmless, may differ on Intel/other Apple Silicon), but `PWMSafeCoordinator` does not depend on it for verification — `DisplayServicesBackend`'s own read-back already works. |
| `PWMSafeCoordinator` pins the real built-in display | pass | Real production pipeline (not a fake backend this time): `appState.pwmSafe = true` → hardware brightness read back at exactly `1.0` → coordinator reports `.pinned`. |
| Disabling restores the exact original brightness | pass | Read back the precise original value (`0.4559...`), not an approximation. |
| Extreme-dim overlay window: exists, correct level/alpha/bounds | pass — **verified via the real window server (`CGWindowListCopyWindowInfo`), not a screenshot** | Screenshots in this session's sandboxed display can't show this: this environment's own front-most pane sits at window layer 1000, and its screenshot mechanism doesn't composite content above itself the way a real monitor would — confirmed by checking the actual window list instead, which is a ground-truth OS query, not a rendering path. At brightness 10% (below the 30% gamma floor): a window owned by the probe process at layer **1001** (one above that layer-1000 pane — `NSWindow.Level.screenSaver + 1`, exactly as CLAUDE.md §3.5 specifies), alpha **0.6667** (`1 - 0.10/0.30`, matching the formula exactly, not approximately), bounds covering the full `1728×1117` screen exactly. |
| Fallback mode: gamma stays at baseline | pass | Read back the exact baseline gamma table while Fallback mode was active with `warmthK=0`: unchanged to 5+ decimal places. |
| Fallback mode: overlay window alpha matches the warmth/brightness formula | pass — same window-list verification | `warmthK=0, brightness=1.0` → alpha **1.0** exactly (`max(1 - 0/6500, 1 - 1.0) = max(1.0, 0.0) = 1.0`), same layer/bounds as the extreme-dim case. |
| PWM-Safe retry/wontHold/drift/toast/disable-restores state machine | pass (14 tests) | Against a fake backend (`DimitTests/PWMSafeCoordinatorTests.swift`), per docs/PLAN.md's explicit C3 requirement. Caught and fixed a real bug while writing these: the drift-polling timer only ever started from `sync()`, but a display doesn't reach `.pinned` until an async verify callback runs *after* `sync()` has already returned — so the poll never started unless `sync()` happened to be called again afterward. The drift test failed exactly this way before the fix. |
| `menu.restore_colours` and Fallback mode toggle in the right-click menu | pass (logic; not clicked live) | Same C1-carried-forward limitation: this session's virtual display can't reliably drive real menu clicks. Verified by construction and by the underlying `restoreColours()`/`fallbackMode` toggle paths, which the integration probe exercises directly. |
| PWM re-pin toast (once per session) | pass (unit test only) | `ToastPresenter` itself (the actual on-screen HUD window) wasn't visually confirmed for the same screenshot-compositing reason as the overlay above; the *trigger logic* (fires once, not on a second drift) is tested in `PWMSafeCoordinatorTests`. |
| External monitor: DDC stub reports unsupported, real monitor's actual brightness backend | **pending** | No external monitor connected this session. `DDCBackend` was a stub (`.unsupported` always) in C3; **C5b implements it for real** — see the C5b section below, and note it is still unverified against a monitor. |
| Auto-brightness banner appears once, dismissable forever | pass (logic only) | `AppState.handleIsOnChanged`/`dismissAutoBrightnessBanner` unit-testable logic; the actual banner UI wasn't clicked live for the same reason as the menu. Confirmed no reliable detection key exists on this macOS 27 beta (`defaults read com.apple.BezelServices dAuto` — no such key), matching CLAUDE.md §3.3's anticipated fallback. |

**Two real, verified findings about hardware/OS behavior, not assumptions**, both documented prominently in code comments so they aren't lost: `CoreDisplayBackend` doesn't actually control the built-in panel's brightness on this Apple Silicon machine (its `get()` is a constant), and the `IORegistry` brightness cross-check CLAUDE.md's own spec describes as "observed on the dev machine" does not track live changes on *this* dev machine as re-tested for this cycle. Both are implemented per spec regardless (harmless, and may behave correctly on hardware this session doesn't have), with the discrepancy flagged for whoever tests on a Studio Display, Pro Display XDR, or Intel Mac.

### Owner-run live test on the real machine, 2026-09-09 (closes three rows that unit tests could not)

The owner ran the Debug build and drove the real UI while the hardware backlight, the live gamma table and the persisted state were sampled together every 1.5–2 s. This is the first evidence for these rows that isn't a unit test or a fake backend.

| Check | Result | Evidence |
|---|---|---|
| Brightness key press → re-pin within 5 s | **pass — was "pending", now closed** | Owner pressed F1 with PWM-Safe on: backlight fell `0.937 → 0.625 → 0.312`, and the app forced it back to `1.000` about 6 s later (poll interval is 5 s). Reproduced three times in two separate traces (`0.937 → 1.000`, `0.812 → 1.000`, `0.312 → 1.000`). |
| Gamma/overlay split at the 30% floor | **pass — was inferred, now observed** | Dragging Brightness below 30% left gamma pinned at exactly `0.300` while the persisted `dim` kept falling to `0.100`; the overlay takes the rest. At 2700K the table read `(0.300, 0.225, 0.150)` — the 2700K multipliers `(1, 0.75, 0.5)` times the 0.30 floor, exact to three decimals. |
| Gamma table tracks AppState with no drift | **pass** | 40 s untouched window: state `dim` and the gamma table agreed on every sample, `g` and `b` exactly `0.000` at 0K. An apparent state/gamma mismatch seen earlier was a sampling artifact — the two values were read seconds apart while the owner was clicking through presets — not a real desync. Re-checked by reading both in the same process. |
| Backlight stability with PWM-Safe **off** | pass | 40 s untouched: exactly `1.000` on every sample, no wandering. No evidence of auto-brightness fighting the app on this machine under steady ambient light. |

One thing deliberately **not** concluded: during a stretch of rapid manual key presses the backlight sat at `0.375` for ~8 s without re-pinning. It did not reproduce in a hands-off window, and the most likely explanation is simply the owner's key presses outpacing the 5 s poll. Recorded here rather than fixed, because the evidence doesn't yet support a change. If a beta tester reports "PWM-Safe lets my brightness drift", start here.

**`/code-review high`, 8 parallel angles, 10 findings, all fixed** (full list in the PR). Test count went 65 → 72. The three that mattered most:

- **Quitting with PWM-Safe on left the backlight stuck at 100%.** Gamma and overlays die with the process; a `DisplayServicesSetBrightness` write does not. `applicationWillTerminate` now calls `restoreAndDisable()`, and so does the "Restore colours" panic button (which previously restored gamma and the overlay synchronously but left the backlight to the deferred `reapply()` — exactly the pipeline-timing dependency that function's own comment says a panic button must not have).
- **The retry path violated CLAUDE.md §3.4's "never call set-brightness more than 4×/second."** Failed pins recursed immediately, firing all three attempts within a few milliseconds. Every retry is now spaced by the same delay as the verify.
- **Adding `fallbackMode` to `PersistedState` would have wiped every existing user's settings on upgrade.** Swift's synthesized `Decodable` throws `keyNotFound` for a missing non-optional key rather than defaulting it — verified empirically against an earlier assumption to the contrary, then fixed with a hand-written `init(from:)` using `decodeIfPresent` for every field. `DimitTests/PersistenceTests.swift` now pins that behaviour so the next added field can't regress it.

Also fixed: stale async verify callbacks from a previous enable landing on a re-enabled session (generation-based invalidation), `sync()` clobbering in-flight pin state on every pipeline pass, pinning a display whose brightness couldn't be read in the first place (nothing to restore on disable), a redundant per-pass rewrite of every overlay window's properties, and a duplicated `NSScreen`-by-`displayID` lookup. Re-ran the full hardware integration probe after the fixes: pin, verify, restore-to-exact-original, overlay geometry and Fallback-mode gamma all still pass on the real display.

## C4 — Settings, hotkeys, login item, onboarding, diagnostics (2026-09-09, same machine)

This cycle is mostly UI, and this session cannot click a popover, a menu or a window. Where a row says "render," it means the SwiftUI hierarchy was hosted off-screen in an `NSHostingView` and rasterized to PNG in all three languages (`DimitTests/LayoutRenderTests`, with `TEST_RUNNER_DIMIT_RENDER_DIR` set) — the same trick used for the popover in C1. Where it says "probe," a real launch of the built app was observed through `CGWindowListCopyWindowInfo` and `defaults`. Everything else needs the owner.

| Check | Result | Notes |
|---|---|---|
| Build and tests | pass | 106 tests (72 → 106). |
| Settings window: layout in en / uz / ru | pass (render) | 520×460 in every language; Russian — the longest — resolves everywhere a translation exists (Запускать при входе, Язык, Проверять обновления автоматически, ДЕНЬ/ВЕЧЕР/НОЧЬ, Теплота/Яркость) and falls back to English exactly where the catalog says `needs_review`. The tab bar itself drew garbled in the off-screen render (a `TabView` sizes its tabs against a window it doesn't have there) — **owner: confirm the three tab titles look right in the real window.** |
| Settings opens from the gear, the right-click menu and ⌘, | pass (by construction, not clicked) | All three paths call the same `SettingsWindowController.show()`; the gear is no longer `.disabled`. Same click limitation as C1. |
| Language override applies live, no relaunch | pass (render + unit test) | SwiftUI surfaces via `.environment(\.locale)` at each root (render). AppKit surfaces — right-click menu, window titles, toast — via `AppState.localized(_:)`: unit test asserts `main.on` → ВКЛ / YOQISH / ON as the override changes. |
| Preset editing, per-preset reset, reset all, live update of the active preset | pass (7 unit tests + render) | The editor's warmth label initially rendered "4 000K" in Russian and "4,000K" in English — SwiftUI's locale-aware integer interpolation — while the popover showed "4000 K" / "4000K". Caught by looking at the render, not by a test; fixed by making `ValueFormatting` the one place either number becomes text, pinned by `ValueFormattingTests`. |
| Onboarding shows on first launch, centred | pass (probe) | Fresh launch (flag unset): one 460×392 window, centred on the visible frame after a fix — the first probe found it at y=33, because `center()` ran on a window SwiftUI hadn't sized yet. |
| Onboarding shows **only** once | pass (probe), **after a real bug** | Sequence: first launch → window; SIGTERM without finishing → flag must stay unset; relaunch → window again; flag set → no window. The first run **failed step 2**: quitting marked onboarding complete, because AppKit fires `windowWillClose` during termination. Fixed by recording completion in `windowShouldClose` (sent for the user's close gesture only) and on Get Started. Flag reset afterwards so the owner still gets the real first run. |
| Onboarding buttons: Next / Get Started / Restore Colours / updates checkbox | **pending** | Need a click. Wiring is direct (`finish()`, `restoreColours`, `$appState.updateChecksEnabled`). |
| Every hotkey works while another app is frontmost | **pending — owner only** | Posting synthetic key events needs Accessibility, which this session doesn't have and the app must never request (CLAUDE.md §1.5). What *is* verified: the six handlers are one-line calls into `AppState.cycleToNextPreset` / `adjustWarmth` / `adjustBrightness`, each unit-tested including clamping at both ends. Owner: with Safari frontmost, press ⌃⌥⌘Z and watch the menu-bar icon flip; then record a shortcut for "Cycle Presets" in Settings and press it. |
| Login item toggles and survives reboot | **pending — owner only** | `SMAppService.mainApp` must be called from inside the running app, and doing it from an unattended session would register a DerivedData path as a login item. Owner: from a copy in /Applications, toggle it in Settings → General, confirm it appears in System Settings → General → Login Items, reboot. |
| No new permission prompts | pass (probe) | Five launches during the onboarding probes, only the onboarding window ever appeared. `KeyboardShortcuts` is Carbon `RegisterEventHotKey`, `OSLogStore(scope: .currentProcessIdentifier)` and `SMAppService.mainApp` need no TCC grant — all three checked against their documentation before use. |
| Diagnostics bundle | pass (unit + real hardware) | `buildText` has six pure tests; `current()` is run once against the real `DisplayManager` and the real backend list and must list every enumerated display. The clipboard write itself needs a click. |
| VoiceOver reads every control in three languages | **pending — owner only** | Labels are present by construction (sliders and icon buttons carry `accessibilityLabel`; Settings/onboarding use native `Toggle`/`Picker`/`Button` whose visible text is their label). Turn VoiceOver on, open the popover and Settings, switch language, listen. |
### Owner-run capture test, 2026-09-09 — the §1.4 promise, verified for the first time

Real Zoom call with screen sharing, plus a QuickTime screen recording, on this machine (macOS 27.0 beta, built-in display), driven by the owner with the v0.2 build.

| Check | Result | Notes |
|---|---|---|
| Filter keeps working on the physical screen during a Zoom share | **pass** | Owner: "when screen shared it was working ok". |
| Zoom viewers see normal, untinted colours | **pass** | The share was not tinted. |
| QuickTime screen recording is not tinted | **pass** | Owner: "in the video no any effects" — no tint in the recorded file. |
| Filter keeps working while a recording is running | **pass** | Initially reported ambiguously ("nothing worked"), then confirmed on re-test: everything worked. `scripts/watch_gamma.swift` exists to make this unambiguous next time — it prints the live gamma table and flags the moment it changes. |

**This is the core product promise** (CLAUDE.md §1.4: "Screenshots, screen recordings and screen-shares must not be tinted") and it now has real-world evidence rather than only the architectural argument that capture reads the pre-LUT framebuffer.

**Scope, stated precisely so this isn't over-claimed:** this exercises the **gamma** path — normal operation at brightness ≥ 30%. It does **not** cover the two paths that use an actual overlay window, which is where the independent review's capture concern actually lives:

- brightness **below 30%**, where the extreme-dim overlay is on screen, and
- **Fallback mode**, which is entirely overlay-driven (and is *expected* to appear in recordings — the UI says so).

Those rely on `sharingType = .none`, which Apple now describes as legacy with no guarantee against ScreenCaptureKit. Zoom and QuickTime honoured it or never hit that path; a ScreenCaptureKit-based recorder might not. Still open, still a C6 beta item — see the review table below.

### Independent review, 2026-09-09 (GPT-5 Astra) — verified findings

A second reviewer with repo access, its own hardware runs and its own probes. Its two patches were read line by line and applied; the P1s it deliberately left unpatched were fixed here. Its report is not in the repo (it lives under the gitignored `build/review/`), so the load-bearing claims are recorded here.

| Finding | Independently reproduced? | Outcome |
|---|---|---|
| **Wake loses the tint.** `lastApplied` claims a tint WindowServer has already wiped, so the diff skips the wake re-apply. | **Yes, by me, on this display.** Tint at 2700K → `CGDisplayRestoreColorSyncSettings()` to stand in for WindowServer → post the real `screensDidWakeNotification` → read the LUT. Green stayed **0.50048876** (neutral) instead of **0.37536657**. Identical to the reviewer's numbers, from an independently written probe. After the fix the same probe restores the tint. | Fixed. This is why the C2 QA row "sleep/wake re-applies" was never closed — it had never actually worked. |
| **Fallback at NIGHT covers the whole screen.** `max(warmthAlpha, brightnessAlpha)` = exactly 1.0 at 0K. | Yes, by arithmetic — and two of our own tests asserted the 1.0 as correct, so they were pinning the bug. | Fixed: red veil capped at 0.70, composited under a black dim veil. 0K keeps 30% of the screen, NIGHT keeps 12%, and dimming at 6500K is now black rather than red. |
| **Brightness write rate.** Only retries were spaced; 3 enable/disable cycles → 6 writes in 0.16 ms. | Yes — and writing the fix surfaced the actual cause: restores fired even where the pin had been deferred or had failed, writing a value the panel already held. | Fixed. One write path; pins wait a per-panel cooldown, restores never do (they run from `applicationWillTerminate`). `PWMRateLimitTests` asserts §3.4 literally against the production interval. |
| **Disconnect loses the original brightness.** Unplug at 40% while pinned to 100%, replug → "original" re-reads as 100%. | Yes, by trace + a new test. | Fixed: remembered values now outlive the connection, and are dropped only when actually restored. |
| **Stale PWM callbacks** (generation reuse after `restoreAndDisable`, no display-membership check). | Accepted on the reviewer's evidence; its three regression tests fail before and pass after. | Fixed by its patch 02. |
| **Capture exclusion isn't guaranteed.** Apple DTS: no public API prevents ScreenCaptureKit capture; `sharingType = .none` is legacy. | Not reproduced — needs a capture matrix across macOS versions. | **Open, and it affects a headline promise** (CLAUDE.md §1.4 "screen-shares must not be tinted"). Not fixable in code. Must be measured in C6 beta before that claim goes on the website. |
| **CoreDisplay can report a false verified pin** on a panel where its getter is constant 1.0 and its setter inert. | Not reproduced — needs a Studio Display / Pro Display XDR / Intel Mac. | **Open.** Matches the constant-1.0 getter already recorded in the C3 rows above. Beta-tester task. |
| **Drift poll pauses during menu tracking** (`scheduledTimer` registers in default mode only). | Not reproduced directly (no menu held open during a poll), but the mechanism is confirmed: the same `.default` vs `.common` distinction was the subject of C5's own `ScheduleCoordinator` review. | **Fixed in C5.** `startPollingIfNeeded()` now builds the `Timer` directly and adds it via `RunLoop.main.add(timer, forMode: .common)`, the identical pattern `ScheduleCoordinator`'s tick timer uses from its own first line. |
| **No sleep-time restore path**; **diagnostics text is English-only**. | Traced, not reproduced. | **Open.** Diagnostics-in-English is deliberate (see the PR); the sleep path is a real gap for C5. |

Its CPU numbers, for the record: idle **0.127%** against a 0.5% budget (passes); state-write → gamma read-back p95 **3.38 ms** against a 50 ms budget, though that stops at the API call, not at photons; **cold popover 180 ms against a 100 ms budget — fails.** Battery was never measured (AC attached throughout), so no battery claim is established either way.

| Stale debug instance | note | The owner's Xcode run of the 0.1 build (PID 69866) was still alive at the end of this session, paused in lldb on a SIGTERM. Stop it in Xcode (⌘.) before running 0.2 — two instances fight over the gamma table and the status bar. |

## C5a — Sunset/sunrise and fixed-times scheduling (2026-09-09, same machine)

| Check | Result | Notes |
|---|---|---|
| Build and tests | pass | 153 tests (118 → 153). |
| NOAA sunrise/sunset for Tashkent, 2026-09-08, within ±3 min of a real reference | pass — **three independent confirmations** | (1) Cross-checked against three public sunrise/sunset calculators for that date before writing any Swift (05:56–05:57 / 18:43–18:45). (2) `SolarCalculatorTests` against that reference, plus a second independent reference (London, 2026-12-21 winter solstice) — added specifically because the algorithm went through two wrong versions that each still "worked" well enough to look plausible on a single reference (see below). (3) The real coordinator, on this machine's real `Calendar.current`/`TimeZone.current` (`Asia/Tashkent`), for **today** (2026-09-09, one day past the written reference): computed 05:57 / 18:43 — matches to the minute. |
| Two wrong formulas caught before either shipped | pass, by construction | First version swapped sunrise and sunset outright (caught by a same-day `sunrise < sunset` test). The "fix" put both events 9+ hours off — still internally consistent (sunrise before sunset, correct day-length seasonality) and would have passed a less careful review. Re-derived from first principles (mean solar noon at the observer's longitude, corrected by the equation of time) rather than patched again; verified in a standalone Python reference before touching Swift. |
| Three-phase model (day/evening/night) merges correctly across midnight | pass (2 tests + real probe) | A query just after midnight correctly finds the previous evening's NIGHT phase rather than nothing; a query at today's sunrise correctly returns to DAY. |
| Manual-override pause/resume, against the real Combine pipeline and a real `Timer` (not just injected-clock unit tests) | pass | Real `AppState` + real `ScheduleCoordinator` (no injected clock), Tashkent, `sunsetToSunrise` mode: (a) the coordinator's own write registered no pause; (b) a subsequent real `appState.warmthK = 5555` correctly recorded a pause anchored to the active phase's start; (c) the real repeating `Timer` ran for 3 real seconds with the paused value held (`5555`) the whole time, confirming production scheduling doesn't fight the pause. |
| Schedule tab renders correctly in all 3 modes × all 3 languages | pass (off-screen render) | Manual/Sunset→Sunrise/Fixed-times, en/uz/ru. The mode picker, city picker (showing the correct localized city name), the collapsed manual-coordinates `DisclosureGroup`, `DatePicker`s and the ramp `Stepper` all render without truncation or wrapping. The Uzbek segmented-picker divider visible mid-render is normal `.segmented` chrome between unselected segments, not truncation — confirmed against the Manual-only render, which shows the same divider with nothing untranslated nearby. |
| "Use my location" requests Location permission only when tapped | pass, by construction | `LocationProvider.requestOneTimeLocation` has exactly one call site, the button's action in `ScheduleSettingsTab`. Not clicked live (this session's standing limitation on real clicks) — CLAUDE.md §8's "no Location prompt unless the user taps the button" is satisfied by there being no other call site to accidentally trigger it from, not by an interactive test. |
| Real sleep/wake interaction with an active schedule; DDC (Opus 5 half, separate PR) | **pending** | Same standing limitations as every prior cycle — needs the owner's hands, or the DDC half specifically needs Opus 5 and the home monitor. |

### 8-angle review — findings and fixes

The first pass (before review) shipped with `target()` deliberately **not** touching ON/OFF, reasoning that an automatic mode switching the filter on unprompted was the worse surprise. The review's altitude angle found the real consequence and it's the most important fix in this cycle:

| Finding | Confirmed how | Fix |
|---|---|---|
| **The schedule could never turn itself back off.** Once a user turned the filter on under a non-manual schedule, there was no state it could ever reach that looked like "off" again — even the sunrise phase just held `isOn=true` at a permanent 4000K tint forever, with zero UI indication why. A mode literally named "Sunset→Sunrise" that can never stop at sunrise defeats the one thing its name promises. | Traced through `Renderer.render`'s `isOn` gate and `PresetStore`'s literal values — a mode-name reading, not a runtime reproduction. | `target()` now controls `isOn`: the schedule's own `.day` phase means genuinely off (neutral, `isOn=false`) once its ramp completes. Manually toggling ON/OFF is treated exactly like a slider drag — a manual override that pauses the schedule until the next boundary. Six new tests in `ScheduleEngineTests`/`ScheduleCoordinatorTests`. |
| **Switching mode live from Settings silently did nothing.** `@Published` fires from `willSet`, before the new value is stored; the config-change handler read `appState.scheduleConfig` synchronously inside the Combine sink and saw the *previous* value. Only a second, unrelated config change would happen to engage it. `DisplayCoordinator` hit this exact bug already and fixed it with `.receive(on: .main)`; the same fix was missing here. | **Reproduced directly** — a temporary probe test confirmed a single live `scheduleConfig` mutation left `warmthK` untouched until a follow-up change. | Added `.receive(on: DispatchQueue.main)` to the `$scheduleConfig` subscription, matching `DisplayCoordinator`'s literal pattern. New regression test drives the coordinator the same way the real UI does: one live mutation, no direct `evaluateNow()` call. |
| **`activePreset` stayed permanently blank once a schedule started.** `warmthK`/`brightness`'s `didSet` unconditionally clears it on every write (C4); a schedule ticking those fields every 10–60s left the popover's DAY/EVENING/NIGHT picker with nothing highlighted for as long as the schedule ran. | Traced the `didSet` chain; confirmed by test. | `evaluateNow()` re-sets `activePreset` last (same pattern `apply(preset:)` uses) whenever a ramp lands exactly on a preset's values; stays `nil` mid-ramp, matching a manual slider drag. |
| **A bedtime earlier than sunset silently broke the night phase**, permanently, for the rest of the night. `phases`/`target` pick the active phase by raw start time; London at midsummer (sunset ≈21:19) with an ordinary 21:00 bedtime let EVENING's later clock time outrank a NIGHT phase that had already begun 19 minutes earlier. | **Reproduced with a real city** (London, 2026-06-21) before being accepted as a real bug, not a contrived edge case. | Phase start times are now forced chronological at generation time (`ScheduleEngine.chronological(_:)`) — any phase that would sort at or before the one before it is nudged one second later. |
| **`PWMSafeCoordinator`'s drift-poll timer still had the exact `.default`-mode bug** `ScheduleCoordinator`'s own code (and this file's prior C4 section) called out as "still open" — a menu held open, or a window being dragged, silently stalls PWM re-pinning. | Not re-reproduced directly in C5 (no menu held open during a poll), but the mechanism is identical to `ScheduleCoordinator`'s own timer, which is tested. | Fixed alongside, one line: `RunLoop.main.add(timer, forMode: .common)`, the same pattern `ScheduleCoordinator` used from its first line. Closes the docs/QA.md item carried forward from C4. |
| `ScheduleConfig`/`TimeOfDay`/`Coordinate` used fully synthesized `Codable` — the next field added to any of them would wipe every existing user's entire saved state, the exact bug class this codebase has already hit and fixed twice. | Traced against `PersistedState`'s own documented history of this bug. | All three now have hand-written `decodeIfPresent`-per-field decoders, matching `PersistedState`'s. |
| No teardown for `ScheduleCoordinator` on quit — a tick already queued on the run loop could fire between `applicationWillTerminate`'s restore calls and the process actually exiting, re-tinting the display on the way out. | Traced; not reproduced (a genuine race requiring precise timing at quit). | Added `ScheduleCoordinator.stop()`, called first in `applicationWillTerminate`, before the gamma-restore calls. |
| Manual lat/lon accepted any parseable-but-invalid value (e.g. "413" instead of "41.3") with no error shown — silently fed into `SolarCalculator`, producing either nonsense times or a spurious "sun never rises." | Traced; plausible fat-finger scenario. | Range-validated (±90 / ±180) before the Apply button enables or the value is stored. |
| A slow permission dialog double-clicked "Use my location" would silently drop the first click's result — `LocationProvider` overwrites its one stored completion with no in-flight guard. | Traced; low severity (the end state is usually still correct). | Button disables itself while a request is outstanding — prevents the double-call at the one real call site rather than adding queueing `LocationProvider` doesn't otherwise need. |
| Three tests asserted the wrong thing after the above fixes (one inverted comparison, one that needed to account for the newly-correct `.receive(on:)` deferral) | Caught by the test suite itself, immediately after each fix. | Fixed in place; both are now correct regression tests for the fixes, not for the bugs. |

One thing the review flagged and this cycle did **not** change: `ScheduleCoordinator`'s manual-override guard (`isApplyingScheduleTick`) is a synchronous flag, not a per-write source tag on `AppState`. The altitude angle noted this is less robust than tagging every write with who made it — correct, but it would mean routing every existing write path (sliders, hotkeys, presets) through a new setter for a currently-hypothetical risk (the guard's one real constraint — the Combine subscription it protects must stay synchronous — is now called out explicitly in both the code comment and `docs/ARCHITECTURE.md`). Revisit if a future change ever needs to debounce that subscription.

## C5b — DDC/CI for external monitors, experimental (2026-09-09, same machine)

**The headline: no external monitor was connected, so the I2C traffic this cycle exists to send has never reached real hardware.** That is not a footnote — it is the defining fact about this cycle, and the reason the feature ships behind a persisted default-OFF flag *and* an "Experimental" label whose own help text says so.

### Verified here

| Check | Result | Notes |
|---|---|---|
| All four `IOAVService` private symbols resolve | pass | `IOAVServiceCreate`, `CreateWithService`, `ReadI2C`, `WriteI2C`, inside the public IOKit framework on macOS 27.0 beta. Probed before writing any code, same discipline as C3. |
| Which IORegistry class actually carries DDC on macOS 27 | pass — **and it isn't the documented one** | `IOMobileFramebufferShim` matches **zero** services here. `DCPAVServiceProxy` is the live path; its `Location` property reads `Embedded` for the built-in panel. The matching code is built on that probe, not on a reference implementation's assumptions. |
| Message framing and checksums against the **VESA DDC/CI 1.1 spec's own worked examples** | pass | Three published vectors reproduce exactly (§6.2 `6E 51 82 F5 01`→`0x49`, §6.3 reply→`0x1D`, §6.4 null→`0xBE`), plus Get-brightness→`0xAC` and Set-brightness-100→`0xCC`. The earlier version of this test recomputed the implementation's own XOR expression, so it could not have caught a wrong seed — which turned out to be the single highest-risk unknown in the file. |
| Inert when the toggle is off | pass | 0.07 ms, no IORegistry walk, no I2C. |
| Never touches the built-in panel, even with the toggle on | pass | `canControl` false in 0.00 ms; resolution order still picks DisplayServices; PWM-Safe still pins the built-in normally with DDC enabled. |
| Cached resolution stays cheap on the pipeline hot path | pass | 200 `canControl` calls in 0.29 ms total. |

### Not verified — and one of these is a stated "Done when"

| Item | Status |
|---|---|
| **Does any of it actually work on a monitor?** Pin, verify, restore, the whole point. | **Unverified.** Needs the owner's home monitor. This is the single thing that would close the cycle. |
| PLAN's C5 "Done when: … **never hangs the UI**" | **Not met, and known.** `readVCP` blocks the main thread for a 50 ms spec-mandated wait per attempt. `set` no longer reads first (the maximum is cached per connection), but a pin still costs one read, and the 5-second drift poll costs one read *forever* while an external monitor is pinned. Bounded deliberately at 2 attempts × 2 checksum seeds rather than MonitorControl's 5 retries, so the worst case is ~200 ms rather than ~500 ms — but on the main actor either way. **Moving DDC transactions off the main actor is a prerequisite for promoting DDC out of Experimental**, which CLAUDE.md §3.4 already schedules for 1.1. |
| Which checksum seed real monitors accept for a *get* request | **Unresolved, and handled by not choosing.** The spec includes the 0x51 source address (giving `0xAC` for brightness); m1ddc and MonitorControl both omit it (`0xFD`) and ship successfully to a lot of hardware. `readVCP` tries the reference seed first, then the spec seed. A request whose checksum a monitor rejects gets no reply at all, so the fallback costs one extra round trip only when the first attempt was already doomed. |
| Multi-monitor DDC | **Deliberately unsupported.** DDC engages only with exactly one external display and one `External` proxy. m1ddc *does* achieve per-display pairing (via `IOObjectConformsTo(…, "IOMobileFramebuffer")`, which still matches 3 nodes here), so this is conservatism rather than a hard limit — but implementing that pairing with no second monitor to test against would mean shipping an untestable guess about *which monitor receives a write*. |
| Real-world DDC quirks | **Known, unhandled, documented.** ddcutil and the Linux kernel driver both auto-detect a "doubled first byte" reply and displays that omit the protocol flag in the length byte. Neither is handled here. Watch for them during hardware testing. |

### Bugs found by review before any hardware saw them

Two independent review passes plus a spec-research pass caught four things that would each have mattered:

- **The I2C read offset was wrong** — passing `0x51` where MonitorControl passes `0`. Verified against its source directly. This alone would have made every read talk to the wrong place.
- **Turning the Experimental toggle off stranded a pinned monitor at 100% permanently**, through quit, recoverable only from the monitor's own OSD. `restoreAndDisable()` re-resolved the backend through `canControl`, which the toggle had just switched off. PWM-Safe now remembers *which backend pinned each panel* and restores through that — undo must never depend on a gate that can close. Same bug class as C3's "backlight left pinned on quit", re-entering through a new door.
- **`get`/`set` didn't enforce what `canControl` promised.** With one external monitor attached, `set(builtInDisplay, …)` would have resolved the *external* monitor's service and written brightness to the wrong panel. Only call ordering prevented it.
- **A read failure was reported to the user as drift.** `checkForDrift` coerced "couldn't read" to 0, which reads as "drifted to zero" — firing the re-pin toast at a user whose brightness hadn't moved and writing to the panel every 5 seconds. Latent for the Apple backends; DDC's flaky reads are what made it reachable.

Also fixed: reply checksums are now validated (seed `0x50`, per spec §6.3 — a corrupted reply could otherwise report a pin as successful while the backlight sat elsewhere); enabling the toggle mid-session used to appear to do nothing, because displays already written off as `.unsupported` were never re-probed; and `DiagnosticsBundle` no longer reports `supportsDDC: false` for a monitor DDC may be actively driving.

## External-monitor session (2026-09-10, same MacBook Pro + Xiaomi "Mi Monitor" 2560×1440 @ 144 Hz, vendor 0x61a9 model 0x2701)

The first time a second display has been connected to the dev machine. It closes the external-monitor rows pending since C2, and it is the first real-hardware evidence for C5b's DDC/CI code — evidence that went the *other* way from what a green test suite suggests, which is exactly why these rows exist. Every number below was read from the OS (`CGGetDisplayTransferByTable`, `DisplayServicesGetBrightness`, raw `IOAVService` I2C), not from a screenshot.

### Display engine on two displays — closed

| Check | Result | Evidence |
|---|---|---|
| Both displays enumerate with stable UUIDs | pass | Built-in id 1 (vendor 0x610), Mi Monitor id 2 (0x61a9/0x2701); UUIDs match what m1ddc lists independently. |
| **0K tints the external monitor through the real pipeline** (C2 "Done when", pending since 2026-09-09) | **pass** | Launched the actual 0.3 binary with `isOn: true, warmthK: 0` seeded; both tables read `(0.500, 0.000, 0.000)` at midpoint — green and blue exactly zero on the Mi Monitor too. At 2700K both read `(0.500, 0.375, 0.250)`, the exact multipliers. |
| Quit restores both displays | pass | SIGTERM → both back to `(0.500, 0.500, 0.500)`; built-in backlight back to its pre-launch 1.0. |
| **Unplug/replug while ON re-applies** (C2 "Done when") | **pass, with timings** | A watcher sampled the external table every 100 ms while the owner replugged: `04.483` display gone → `09.790` back with the stale 2700K table → `10.030` WindowServer resets it to neutral → `10.135` **Dimit re-applies 2700K, 105 ms after the reset** (the 300 ms debounce is the ceiling, not the typical). Dimit survived the disconnect without a crash or a stuck table. |
| Both displays follow one set of controls | pass, by design | Owner noticed changing warmth on either screen changes both and asked whether that's right. It is: CLAUDE.md §1.1 and §3.3 — one state, all displays; per-display overrides are a 1.1 item (§10). |
| Backlight pin/restore on the built-in with a second display attached | consistent, not conclusive | Read 1.0 before, during and after — the owner runs the panel at 100%, so the pin was a no-op. C3's rows already proved pin and exact restore from 0.4559. |

### DDC/CI against a real monitor — the feature does not work on this one, and Dimit says so correctly

| Check | Result | Evidence |
|---|---|---|
| Service resolution (`DDCLink.singleExternalService`) | pass | Exactly one `DCPAVServiceProxy` with `Location == External` (plus the `Embedded` one); service created in 0.3 ms. The IORegistry matching built blind in C5b is right. |
| Monitor speaks DDC/CI at all | **yes** | Idle reads return the spec §6.4 null message `6e 80 be` repeating, with a correct checksum (seed 0x50). Requests change what comes back. This is a DDC/CI endpoint, not a dead bus. |
| `readVCP(0x10)` through the production code | **nil, every time** | 2 seeds × 2 attempts, 232 ms each, all four rejected by `parseVCPReply`. |
| What the monitor actually sends to Get VCP | a constant "no" frame | `6e 85 c2 00 00 03 00 71` — **identical** for brightness, contrast, VCP version (0xDF), input source, power, a bogus code 0x99, and a request with the length flag stripped. A monitor that implemented Get VCP would answer differently per code. Interpreted as an error/unsupported frame, not data. |
| Which checksum seed this monitor accepts | **the spec's, not the reference implementations'** | Requests seeded 0x6E^0x51 (spec, ddcutil) get the null message = "parsed, nothing to say"; requests seeded 0x6E alone (m1ddc, MonitorControl) get the `c2` error frame. Same for Set: spec seed → null (acknowledged), reference seed → error frame. One monitor, one data point, but it is the opposite of what both references assume — `readVCP` tries both, so nothing changes; recorded in `DDCController.swift`. |
| Set VCP 0x10 = 100 (the exact PWM-Safe command, spec vector checksum `0xCC`) | acknowledged by the bus; **effect not clearly established** — a later 20→100→20→100 write sequence (both seeds) got an "I think working ok" from the owner, not a definite dim/brighten observation, and the OSD's DDC/CI setting was never confirmed. Treat writes as unverified until someone watches the panel during that sequence. | `IOAVServiceWriteI2C` returned success and the monitor answered null. Whether the panel actually went to 100% only the OSD can say — **owner: check the OSD's brightness value and whether it has a DDC/CI toggle (Xiaomi ships one, often off).** If DDC/CI was off, this whole table is "off" behaviour and needs re-running once. |
| **Reference implementation cross-check: m1ddc 1.2.0 reads garbage** | the strongest result of the session | `m1ddc get luminance` → **110**, `get contrast` → **110**, `max luminance` → **−128**. It parses that same constant frame at fixed byte offsets with no checksum or opcode check. Dimit's `parseVCPReply` validates the reply checksum (seed 0x50), the 0x02 opcode, the result byte and the echoed VCP code — all added by the C5b review before any hardware was seen — and therefore reports `unsupported` instead of pinning PWM-Safe against a fictitious "110". |
| DDC toggle ON with this monitor, in the real app | `unsupported`, cleanly, no hang | Full 0.3 launch with `ddcEnabled: true, pwmSafe: true`: pipeline ran, both displays tinted, nothing logged, no stall visible. Resolution is cached as `.unusable` per connection, so the cost is paid once. |
| Main-thread cost of a non-answering monitor | **232 ms once per connection**, measured | 4 attempts × (50 ms spec wait + ~8 ms I2C). This is the "never hangs the UI" clause PLAN's C5 "Done when" still fails; the fix (DDC off the main actor) stays a prerequisite for leaving Experimental. |

**Net for C5b's "Done when":** "pins the home monitor to 100% or shows `unsupported` cleanly" — closed on the `unsupported` branch, for real this time. "Never hangs the UI" — still not met (232 ms, above). Whether any Mi Monitor can be driven at all depends on the OSD question above.

**Housekeeping:** `m1ddc` was installed via Homebrew for the cross-check (`brew uninstall m1ddc` to remove). The owner's saved state was backed up before the seeded launches and restored after; Dimit was quit with SIGTERM and both displays verified neutral afterwards.

### Schedule active across display sleep/wake, two displays (2026-09-10, 01:29 local)

| Check | Result | Evidence |
|---|---|---|
| Sunset→sunrise schedule (Tashkent) turns the filter on by itself at night | **pass** | Seeded `isOn: false`, mode `sunsetToSunrise`; 5 s after launch both displays read `(0.200, 0.000, 0.000)` — NIGHT's 0K at 40% dim exactly — and the persisted state showed `isOn: true, warmthK: 0, brightness: 0.4, activePreset: "night"`. The C5a review's "schedule controls ON/OFF" and "activePreset re-set when a ramp settles" fixes both hold on real hardware. |
| Display sleep → wake with the schedule active | pass (display-sleep path) | `pmset displaysleepnow`, 10 s, `caffeinate -u` to wake. The external display leaves the active list during display sleep and returns already tinted `(0.200, 0, 0)` (no neutral sample at 100 ms resolution — either WindowServer doesn't reset on display-sleep wake, or the re-apply beat the sampler). Built-in identical. |
| Full system sleep → wake with the schedule active | **pass — owner-run, 2026-09-10** | Owner slept and woke the Mac with the schedule on and both displays attached: "sleep wake working". Closes the row that C2 opened and the C4 review flagged as never actually closed. |
| Quit restores both displays; owner's saved state restored afterwards | pass | Both `(0.500, 0.500, 0.500)` after SIGTERM; `state.v1` re-imported from the pre-test backup. |

## C6 — release scripts and the beta artifact (2026-09-10, same machine, ad-hoc signed)

No Developer ID certificate and no notarization credentials exist on this machine yet (`security find-identity -v -p codesigning` → 0 identities), so everything below is the **ad-hoc** path. The Developer ID path in the same scripts is written against Apple's documented `xcodebuild archive` / `-exportArchive` / `notarytool` flow and cannot be exercised until the account holder installs the certificate — docs/RELEASE.md §0.

| Check | Result | Evidence |
|---|---|---|
| `scripts/build.sh` produces a universal Release app | pass | `lipo -archs` → `x86_64 arm64`; version `0.4 (2026091001)` (build number from the date, CLAUDE.md §7); `codesign --verify --deep --strict` satisfied. |
| Hardened runtime on, no entitlements | pass | `codesign -dvv` flags `0x10002(adhoc,runtime)`; `--entitlements :-` prints an empty list. (The script's first version mis-read the older `codesign` output form and warned falsely; fixed to check for `<key>`.) |
| `scripts/build_dmg.sh` produces a mountable image with the app + Applications link | pass | 1.3 MB UDZO; mounted read-only, listed `Applications Dimit.app`, signature verified *inside* the image, detached. Built with `hdiutil`, not create-dmg — deviation and reason in the script header. |
| The Release build launches and quits cleanly | pass | `open build/release/Dimit.app` → process alive after 3 s → SIGTERM → gone. |
| `scripts/notarize.sh` | written, **not run** | Refuses ad-hoc builds by design; needs the Developer ID build and a `notarytool` keychain profile (docs/RELEASE.md §0). |
| Fresh-browser download opens on another Mac using the published instructions | **pending — tester** | The C6 "Done when" line that only a tester can close; the instruction text is in docs/TESTING_CHECKLIST.md and docs/RELEASE.md §3. |
| At least one row per macOS major the testers own | **pending — testers** | Table below fills as reports arrive. |

### Capture matrix (C4 review item; the site may only claim what this table shows)

| Capture method | Gamma path (brightness ≥ 30%) | Extreme-dim overlay (< 30%) | Fallback mode |
|---|---|---|---|
| ⌘⇧4 screenshot | **not tinted** — owner + C2 probe, macOS 27 | pending | pending (expected: tinted; the UI says so) |
| ⌘⇧5 / QuickTime recording | **not tinted** — owner, macOS 27 (v0.2) | pending | pending |
| Zoom screen share | **not tinted** — owner, macOS 27 (v0.2) | pending | pending |
| Google Meet / Teams | pending | pending | pending |
| ScreenCaptureKit recorder (OBS or similar) | pending | pending | pending |

The two overlay columns rely on `sharingType = .none`, which Apple describes as legacy with no guarantee against ScreenCaptureKit (docs/QA.md § Independent review). Until they have rows, the download page says "screenshots and screen shares stay normal at brightness 30% and above; below that, and in Fallback mode, they may show the effect" — not the unqualified claim.

### Tester reports (one row per Mac)

| Date | Tester | Mac | macOS | Display(s) | Result | Notes |
|---|---|---|---|---|---|---|
| | | | | | | |

## Performance

| Date | Version | Machine | Idle CPU (5 min avg) | Popover open (ms) | Slider latency |
|---|---|---|---|---|---|
